# API Routes

Use this folder for Next.js route handlers only.

- Put handlers in `src/app/api/**/route.ts`
- Keep shared request clients in `src/lib/api`
- Keep feature-specific server helpers close to the feature when they are not shared
